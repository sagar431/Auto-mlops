# Fast Training Configuration
# Quick experiments with fewer epochs

epochs: 3
batch_size: 128
learning_rate: 0.001
optimizer: adam
weight_decay: 0.0
scheduler:
  name: reduce_on_plateau
  patience: 2
  factor: 0.1
