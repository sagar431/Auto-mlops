# Long Training Configuration
# Extended training for better accuracy

epochs: 50
batch_size: 64
learning_rate: 0.001
optimizer: adam
weight_decay: 0.0001
scheduler:
  name: cosine
  T_max: 50
  eta_min: 0.00001
