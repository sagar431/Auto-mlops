# Default training configuration
epochs: 100
batch_size: 128
learning_rate: 0.001
optimizer: adam
weight_decay: 0.0001
num_workers: 0

early_stopping:
  patience: 10
  min_delta: 0.0001

scheduler:
  name: reduce_on_plateau
  factor: 0.5
  patience: 5
