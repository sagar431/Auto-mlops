# Model Artifacts

Place your exported TorchScript model and class labels here.

Required files:
- `model.ts` (TorchScript model)
- `classes.txt` (one class label per line)

The model API expects these paths by default:
- `MODEL_PATH=/app/models/model.ts`
- `CLASSES_PATH=/app/models/classes.txt`
