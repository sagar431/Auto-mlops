apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auto-mlops.fullname" . }}-secret
  labels:
    app.kubernetes.io/name: {{ include "auto-mlops.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  MODEL_API_KEY: {{ .Values.secrets.modelApiKey | quote }}
