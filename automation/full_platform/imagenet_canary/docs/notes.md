# Notes

- Replace the example HuggingFace model IDs with any three ImageNet classifiers.
- For EKS/ALB, adjust the Ingress annotations in `k8s/ingress.yaml`.
- The AnalysisTemplate assumes Prometheus is reachable at `monitoring` namespace.
